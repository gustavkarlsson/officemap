plugins {
    id "com.moowork.node" version "0.10"
}

apply plugin: 'com.moowork.node'

task serve(dependsOn: ':webapp:npmInstall', type: NodeTask) {
    script = file('app.js')
    ignoreExitValue = true
}