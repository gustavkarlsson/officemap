task copyWeb(type: Copy, dependsOn: ":web:build") {
    from "web/dist"
    into "install/web"
}

task copyServer(type: Copy, dependsOn: ":server:installDist") {
    from "server/build/install/server"
    into "install"
}

task install(dependsOn: ["copyServer", "copyWeb"])
